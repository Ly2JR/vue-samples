/* jshint esversion:6 */
const IfElseTemplateApp = {
    data() {
        return {
            ok: true
        };
    },
};

const app = Vue.createApp(IfElseTemplateApp);
app.mount('#if-else-template');